# Public URL of the (self-hosted) Convex backend the browser connects to.
# Host-visible Convex API origin. Copy to .env.local and fill explicitly.
NEXT_PUBLIC_CONVEX_URL=
FRONTEND_ORIGIN=
CORS_ALLOWED_ORIGINS=
TELEGRAM_API_ORIGIN=
